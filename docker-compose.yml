services:
  housing-api:
    build:
      context: .
      dockerfile: Dockerfile.api
    ports:
      - "8000:8000"

  housing-streamlit:
    build:
      context: .
      dockerfile: Dockerfile.streamlit
    ports:
      - "8501:8501"
    depends_on:
      - housing-api
